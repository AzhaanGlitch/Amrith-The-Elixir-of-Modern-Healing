# Core Flask API
flask>=3.0.0
flask-cors>=5.0.0
gunicorn>=22.0.0
python-dotenv>=1.0.0

# Data / ML
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.5.0
Pillow>=10.0.0

# Hugging Face (text guidance model)
transformers>=4.40.0
huggingface-hub>=0.20.0

# NOTE: tensorflow-cpu and torch (CPU-only) are installed
# directly by the Dockerfile to save ~4GB of CUDA dependencies.
# Do NOT add tensorflow or torch here — it will override the
# CPU-only installs and break the deployment.
